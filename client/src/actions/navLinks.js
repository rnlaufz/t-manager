import {DASH_TITLE, ADD_TITLE, SETTINGS_TITLE, COMPLETED_TITLE, EDIT_TITLE} from './types';


// @TO_DO finish action of setting secNav title 
export const setNavTitle = (title) => dispatch => {
    return true 
}